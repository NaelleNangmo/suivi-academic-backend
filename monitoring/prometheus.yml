global:
  scrape_interval: 5s

scrape_configs:

  # métriques système
  - job_name: "node"
    static_configs:
      - targets: ["node-exporter:9100"]

  # métriques Laravel
  - job_name: "laravel"
    metrics_path: /metrics
    static_configs:
      - targets: ["nginx:80"]   # on passe par nginx
